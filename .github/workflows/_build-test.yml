name: _build-test

# Reusable build + test pipeline (workflow_call only) — the single source of
# truth for "turn the 9 service repos into a tested mega-RPM". Called by:
#   build.yml   (CI validation: version = CalVer date, artifact is CI-only)
#   release.yml (releases:      version = tag, artifact gets published)
# NOTE: caller `env:` does NOT propagate into a reusable workflow — everything
# needed is defined here.

on:
  workflow_call:
    inputs:
      version:
        description: "RPM version. Empty = CalVer date (CI dev builds)."
        type: string
        default: ""
      artifact-name:
        description: "Name for the uploaded RPM (+ MANIFEST.txt) artifact."
        type: string
        required: true
      services-ref:
        description: "Git ref to check out for source-built services (frontend always; all services when source=local)."
        type: string
        default: "main"
      source:
        description: "Backend binaries: 'local' (compile from source) or 'release' (download GitHub Releases per services.lock)."
        type: string
        default: "local"
      checkout-ref:
        description: "Ref of THIS repo to build from (empty = triggering sha). Release re-publish passes the tag."
        type: string
        default: ""

permissions:
  contents: read

env:
  DUYNHLAB_SRC_ROOT: ${{ github.workspace }}/src
  CONTAINER_RUNNER: docker

jobs:
  build:
    name: Build mega-RPM
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout packages repo
        uses: actions/checkout@v7
        with:
          path: packages
          ref: ${{ inputs.checkout-ref }}

      - name: Set up Go (GOTOOLCHAIN=auto upgrades as needed per service)
        if: inputs.source != 'release'
        uses: actions/setup-go@v6
        with:
          go-version: "stable"
          check-latest: true

      - name: Set up Node (for frontend)
        uses: actions/setup-node@v6
        with:
          node-version: "20"

      - name: Install host tools
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y -qq gettext-base curl tar
          # No yq: the service registry is hardcoded in scripts/lib/common.sh.

      - name: Resolve version
        run: |
          v="${{ inputs.version }}"
          [ -n "$v" ] || v="$(date -u +%Y.%m.%d)"
          echo "VERSION=$v" >> "$GITHUB_ENV"
          echo "Building version $v"

      - name: Fetch backend release binaries (source=release)
        if: inputs.source == 'release'
        working-directory: packages
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: ./scripts/fetch-releases.sh

      - name: Fetch service source (frontend always; all repos when source=local)
        working-directory: packages
        run: |
          set -euo pipefail
          if [ "${{ inputs.source }}" = "release" ]; then
            ./scripts/fetch-sources.sh "${{ inputs.services-ref }}" static
          else
            ./scripts/fetch-sources.sh "${{ inputs.services-ref }}"
          fi

      - name: Build services from source (frontend always; backends when source=local)
        working-directory: packages
        # fetch-sources.sh already checked out the requested services-ref; tell
        # build-local.sh to build the tree as-is instead of re-syncing to main
        # (without this it force-checkouts main and silently overrides the ref).
        env:
          DUYNHLAB_NO_GIT: "1"
        run: |
          set -euo pipefail
          for svc in $(bash -c '. scripts/lib/common.sh && svc_list'); do
            type=$(bash -c ". scripts/lib/common.sh && svc_field $svc type")
            if [ "${{ inputs.source }}" = "release" ] && [ "$type" = "backend" ]; then
              echo "skip build-local $svc (staged from release)"
              continue
            fi
            echo "::group::build-local $svc"
            ./scripts/build-local.sh "$svc" "$VERSION"
            echo "::endgroup::"
          done

      - name: Render systemd units
        working-directory: packages
        run: ./scripts/render-systemd.sh

      - name: Stage Source0 tarball (writes the composition manifest)
        working-directory: packages
        run: ./scripts/stage-all.sh

      - name: Build mega-RPM (rpmbuild in container)
        working-directory: packages
        env:
          BUILD_RUNNER: docker
        run: ./scripts/build-rpm.sh

      - name: Collect artifact (RPM + manifest)
        working-directory: packages
        run: |
          set -euo pipefail
          cp build/staging/opt/duynhlab/etc/manifest dist/MANIFEST.txt
          ls -lh dist/

      - name: Upload artifact
        if: success() || failure()
        uses: actions/upload-artifact@v7
        with:
          name: ${{ inputs.artifact-name }}
          path: |
            packages/dist/*.x86_64.rpm
            packages/dist/MANIFEST.txt
          if-no-files-found: error
          retention-days: 14

  # File-level install test of the built RPM, in parallel across the EL9 rebuilds
  # we support (Rocky + Alma). Reuses the artifact from `build` — no rebuild.
  install-test:
    name: Install test (${{ matrix.image }})
    needs: build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    strategy:
      fail-fast: false
      matrix:
        image: [rockylinux:9, almalinux:9]
    steps:
      - name: Checkout packages repo
        uses: actions/checkout@v7
        with:
          path: packages
          ref: ${{ inputs.checkout-ref }}

      - name: Download RPM artifact from build job
        uses: actions/download-artifact@v8
        with:
          name: ${{ inputs.artifact-name }}
          path: packages/dist

      - name: Install test (${{ matrix.image }})
        working-directory: packages
        env:
          TEST_IMAGE: ${{ matrix.image }}
        run: ./scripts/test-install.sh
