#!/usr/bin/env bash
# duynhpass — generate a cryptographically random password.
# Usage: duynhpass [length]   (default 32)
set -eu
LEN=${1:-32}
[[ "$LEN" =~ ^[0-9]+$ ]] && [[ "$LEN" -ge 8 ]] || {
  echo "Usage: $0 [length>=8]" >&2; exit 2;
}
# Avoid pipefail: tr receives SIGPIPE when head closes its stdin.
LC_ALL=C tr -dc 'A-Za-z0-9' </dev/urandom 2>/dev/null | head -c "$LEN"
echo
